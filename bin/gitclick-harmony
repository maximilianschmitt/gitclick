#!/usr/bin/env node

var iojs = require('is-iojs');

var spawn = require('child_process').spawn;
var args = ['bin/gitclick'].concat(process.argv.slice(2));
var program = iojs ? process.argv[0] : './node_modules/.bin/babel-node';

spawn(program, args, { stdio: [0, 1, 2] });